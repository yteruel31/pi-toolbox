---
"@yteruel31/pi-subagents": patch
---

Fix Jev rejecting available routes when a named-agent tool allowlist includes extension tools. Preserve the allowlist and fixed routing constraints, distinguish categorical routing conflicts without exposing private inputs, and persist failed routing instead of leaving runs pending.
