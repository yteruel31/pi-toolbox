---
---

Prepare the initial public releases of all eight new `@yteruel31/*` package identities. Existing workspace versions become their first scoped npm versions, so this setup changes no already-published package version.
