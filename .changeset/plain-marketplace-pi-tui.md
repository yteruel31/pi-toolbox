---
"@yteruel31/pi-claude-marketplace": minor
---

Move the exact `@earendil-works/pi-tui` runtime pin from 0.84.2 to 0.87.1, matching the Pi version the rest of the toolbox is built and tested against. The pin is exact, so this raises the version installed alongside the package.
