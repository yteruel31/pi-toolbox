---
"@yteruel31/pi-web-access": minor
---

Add a Diagnostic tab to Web Access setup with local-only prerequisite checks, conservative platform-specific remedies and an explicit offline production-sandbox render test. Distinguish sanitized browser failures and join cancelled launches before cleanup, without changing classic HTTP fetch or relaxing isolation.
